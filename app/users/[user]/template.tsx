'use client'
import React from 'react'

function template({ children }: { children: React.ReactNode }) {


    return (
        <div className=''>
            {children
            }</div>
    )
}

export default template